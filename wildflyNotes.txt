politicas encontradas en login-config.xml
1. jbossmq
2. aritecPolicy
   ldap
3. JBossBDD
    a nivel de tabla
4. JBossBankBDD
    a nivel de tabla
5. ptcPolicy
    a nivel de bd con SPs.

colas encontradas
jbossmq-destinations-service.xml
1. testTopic
   securedTopic
   testDurableTopic
   testQueue
   colas de ejemplo por default: queue/A, queue/B, queue/C, queue/D, queue/ex, MyQueue
   
jbossmq-service.xml   

1. configuracion de seguridad   
java:/jaas/jbossmq   

observaciones por proyecto.
1. accounting-ejb.jar
   @SecurityDomain("java:/jaas/ptcPolicy")

2. bank-daos.jar
   no se encontro nada, solo son clases planas.

3. bank-ejb.jar
   @SecurityDomain("java:/jaas/ptcPolicy")

4. credits-ejb.jar
   @SecurityDomain("java:/jaas/ptcPolicy")

5. inventory-ejb.jar
   @SecurityDomain("java:/jaas/ptcPolicy")

6. pos-ejb.jar
   @SecurityDomain("java:/jaas/ptcPolicy")   

   
iniciar wildfly
para iniciarlo con soporte para colas. Utiliza por default el ActiveMQ.

standalone.cmd -c standalone-full.xml

   
